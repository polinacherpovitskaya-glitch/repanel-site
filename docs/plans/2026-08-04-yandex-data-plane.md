# План: единый контур RePanel site в Yandex Cloud

Спецификация: [`../specs/2026-08-04-yandex-data-plane.md`](../specs/2026-08-04-yandex-data-plane.md)

## 1. Сохранение исходного состояния

- [x] Снять новые полные экспорты Supabase и YDB без вывода секретов в логи.
- [x] Составить канонический JSON-манифест по всем десяти коллекциям: количество,
      размер и SHA-256.
- [x] Сохранить конфигурационный манифест бакетов и число объектов.
- [x] Загрузить экспорты в приватный версионируемый backup-бакет Yandex.
- [x] Скачать копии обратно и повторно проверить SHA-256.

## 2. PostgreSQL и серверный слой

- [x] Добавить идемпотентную SQL-схему `site_records` с ограничением коллекций и
      уникальными индексами.
- [x] Реализовать серверный PostgreSQL-адаптер для фактически используемого
      PostgREST-подмножества.
- [x] Заменить `SupabaseClient` в серверных типах и вызовах на локальный тип БД.
- [x] Перевести страницы каталога на серверную БД.
- [x] Добавить read-only `/api/catalog/products` и перевести апселл корзины на
      этот API.
- [x] Добавить `/api/health` с проверкой БД и режима запрета записи.
- [x] Добавить серверную защиту мутаций через `CUTOVER_WRITE_FREEZE`.

## 3. Yandex Object Storage

- [x] Добавить серверный клиент S3-compatible Object Storage.
- [x] Перевести административную загрузку изображений с Supabase Storage.
- [x] Ограничить допустимые типы, размер и ключи объектов как сейчас.
- [x] Обновить Next image remote patterns на точный домен Yandex Storage.

## 4. Удаление runtime-зависимостей

- [x] Удалить публичный Supabase-клиент и перенести типы магазина в нейтральный
      модуль.
- [x] Удалить Supabase/YDB runtime-пакеты и build args.
- [x] Обновить проверку российского деплоя под PostgreSQL/Object Storage.
- [x] Оставить исторические скрипты только там, где они нужны для проверяемого
      one-shot экспорта, либо заменить их нейтральным импортом.
- [x] Проверить, что клиентские артефакты не содержат старых URL/ключей.

## 5. Автоматические проверки

- [x] Добавить unit-тесты фильтров, проекции и write freeze.
- [x] Добавить тесты API опубликованного каталога и health.
- [x] Выполнить targeted lint, `npm test` и production build.
- [x] Выполнить контейнерный smoke на теневой VM с отдельным PostgreSQL.

## 6. Теневой контур Yandex

- [x] Создать отдельную роль и базу `repanel_site` в существующем PostgreSQL.
- [x] Создать версионируемый бакет изображений и минимальный сервисный аккаунт.
- [x] Собрать amd64-образ с неизменяемым тегом commit SHA.
- [x] Развернуть контейнер с read-only rootfs, лимитом памяти и внутренним портом.
- [x] Подключить теневой домен через Caddy и проверить TLS.

## 7. Импорт и верификация

- [x] Импортировать исходные документы без преобразования полей.
- [x] Сравнить количества и канонические SHA-256 по каждой коллекции.
- [x] Снять `pg_dump`, загрузить в backup-бакет и проверить скачивание.
- [x] Восстановить дамп во временную БД и повторить сверку.
- [x] Проверить существующие товары, два заказа и четыре события истории.
- [x] Выполнить обратимые create/update/delete smoke-тесты только на тестовой
      записи теневого контура.

## 8. Переключение и наблюдение

- [x] Исключить старый write-path из production DNS, снять финальную дельту и
      повторить сверку.
- [x] Переключить `site.re-panel.ru` на VM, не меняя Vercel-редиректы.
- [x] Проверить TLS, страницы, админку, checkout validation и webhook endpoint.
- [x] Перевести прежний Serverless Container в rollback-only состояние.
- [x] Подтвердить ежедневный backup и успешное контрольное скачивание.
- [ ] Наблюдать новый контур 14 дней до отдельного решения об отключении старых
      ресурсов.

### Протокол переключения

- Production DNS переключён 2026-08-05: `site.re-panel.ru A 158.160.27.182`,
  TTL 300. Vercel-домены `re-panel.ru` и `www.re-panel.ru` не менялись.
- Финальный источник повторно сверён перед DNS: 7 товаров, 2 заказа и 4 события
  истории; канонические SHA-256 всех десяти коллекций совпали с PostgreSQL.
- Финальный архив сохранён в приватном версионируемом backup-бакете по префиксу
  `site/m5/final-source/20260805T044135Z/` и проверен после скачивания.
- Production работает на immutable-образе `ro-repanel-site:c4170d148524`;
  `/api/health` сообщает healthy DB и разрешённую запись.
- Caddy выпустил отдельный сертификат Let's Encrypt. Главная, privacy, каталог,
  карточка товара, админка, checkout validation и webhook health прошли smoke.
- Старый API Gateway/Serverless Container, Supabase и YDB сохранены без удаления
  только для аварийного rollback на период наблюдения.
